# Security implementation for Denis Alexandrescu Portfolio

# Environment Variables
# NEVER commit the actual .env file to version control.
# Use this file as a template.

# If you add an API for contact forms or AI features:
# CONTACT_EMAIL=your-secure@email.com
# GEMINI_API_KEY=your-api-key-here (Initialized server-side for protection)

# Port configuration (Hardcoded to 3000 by platform)
PORT=3000

# Security Headers (Handled by server.ts via Helmet)
# Rate Limiting (Handled by server.ts via express-rate-limit)
